import { deleteReleaseAssets } from './delete-release-assets';

describe('deleteReleaseAssets', () => {
  it('should work', () => {
    expect(deleteReleaseAssets()).toEqual('delete-release-assets');
  });
});
